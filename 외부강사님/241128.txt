프론트엔드 = 리액트

html = 노드 구조

노드구조는 ? -> 상하관계 트리구조

자바스크립트 알려면
BOM : window
DOM : document

웬만한 내장객체는 window 부터 시작

상수는 전체대문자에 언더스코어


이벤트 //////// 자바스크립트, http
모두소문자

on 붙으면 다 이벤트 ex) onclick onmouseover ....

단수 복수 차이점

단수 = 1개 아니면 null
복수 = 0개 이상

변수명은 무조건 문자여야됨 (특문에서는 _ $ 는 가능($==시스템관련된것))

자바스크릡트는 무조건 동적이다. 매개변수는 옵션이라 들어가도되고 안들어가도됨

http 메서드 get post fetch del put

form{get}
?기준으로 왼쪽 요청주소 오른쪽이 keyvalue 쿼리스트링타입으로 url
head만 있음
from{post }
body가 있음 폼데이터로 보냄

ajax

반드시 키값은 "묶여있어야됨 스트링도 "임